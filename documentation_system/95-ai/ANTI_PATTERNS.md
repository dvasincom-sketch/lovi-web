# ANTI_PATTERNS

Status: canonical
Owner: Dmitry Vasin
Last Updated: 2026-05-14

## Insalon
- Не использовать `records.service_cost` для выручки
- Не считать `service_cost = 0` отсутствием выручки (абонемент/сертификат)
- Не смешивать личные расходы с бизнесом (разметка project)
- Не считать выплаты 1-го числа текущим месяцем
- Не использовать дату платежа для аренды/Fitmost — нужен period
- Не забывать пагинацию Supabase (fetch_all)
- Не считать наличные авансом мастеру
- Не использовать `dataPointSelection` в ApexCharts, только `markerClick`
- Не строить расписание только из records
- В Tabler UI не использовать `bootstrap.Modal`

## Lovi — код
- Не хардкодить цвета — только CSS-переменные
- Не хардкодить список услуг — читать из service_strategies где status=published
- Не использовать seance_length из YCLIENTS для длительности — только duration_min из Supabase
- Не дублировать import в JSX файлах — проверять перед добавлением
- Иконки только через lucide-react
- Именование: PascalCase компоненты, camelCase утилиты
- Не трогать существующий контент без явного запроса

## Lovi — AI-взаимодействие
- Не регенерировать весь файл ради точечного изменения (< 300 символов → терминал)
- Не логировать dev session до закрытия сессии владельцем
- Не предлагать решение до того как понята проблема — сначала диагностика
- Не делать несколько правок подряд без проверки результата
- Не использовать sed для многострочных замен в zsh — использовать /tmp/fix.py
